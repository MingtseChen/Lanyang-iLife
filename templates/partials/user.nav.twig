{#TODO add hover effect on dropdown#}
<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark {{ nav_color=="trans" ? 'bg-transparent' : 'bg-dark' }} fixed-top">
    <div class="container">
        <a class="navbar-brand" href="{{ path_for('home') }}">iLanyang</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <!-- Left navbar links -->
            {% if _session.group == 0 %}
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path_for('admin') }}">管理界面</a>
                    </li>
                </ul>
            {% endif %}
            <!-- Right navbar links -->
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">

                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link" data-toggle="dropdown" href="#">
                        交通車預約
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path_for('busIndex') }}">交通車預約</a>
                        <a class="dropdown-item" href="{{ path_for('userBus') }}">我的交通車</a>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link disabled" href="#">大三出國通報</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path_for('repair') }}">線上報修</a>
                </li>
                <li class="nav-item">

                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link" data-toggle="dropdown" href="#">
                        蘭陽掛包
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path_for('userPackage') }}">我的掛包</a>
                        <a class="dropdown-item" href="{{ path_for('lostFound') }}">掛包招領</a>
                    </div>
                </li>
                <!-- Authentication Links -->
                {% if _session.name %}
                    <li class="nav-item dropdown">
                        <a class="nav-link" data-toggle="dropdown" href="#">
                            Hi, {{ _session.name|default('') }}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ path_for('userIndex') }}">個人設定</a>
                            <a class="dropdown-item" href="{{ path_for('logout') }}">登出</a>
                        </div>
                    </li>
                {% else %}
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path_for('loginForm') }}">登入</a>
                        </li>
                    </ul>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
<!-- /.navbar -->